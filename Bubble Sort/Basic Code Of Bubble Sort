#include <stdio.h>
int main ()
{
    int n;
    scanf("%d",&n);
    int ar[n];
    for (int i = 0; i < n; i++)
    {
        scanf("%d",&ar[i]);
    }

    for (int i = 0; i < n-1; i++)
    {
        for (int j = 0; j < n-1-i; j++)
        {
            if (ar[j] > ar[j+1]) // compare value of two index
            {
                int temp = ar[j];  // swaping
                ar[j] = ar[j+1];
                ar[j+1] = temp ;
            }
            
        }

    }

    for (int i = 0; i < n; i++)
    {
       printf("%d ",ar[i]);
    }
    printf("\n");
       
    return 0;
}
